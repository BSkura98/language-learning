import styled from 'styled-components';

import { PageWrapper } from '../../components/PageWrapper/styled';

export const FinishedRepetitionsPageWrapper = styled(PageWrapper)`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 5rem;
`;
